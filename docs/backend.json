{
  "entities": {
    "UserProfile": {
      "title": "UserProfile",
      "type": "object",
      "description": "Stores individual user preferences, settings, and overall account information.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the UserProfile entity, matching the Firebase Authentication user ID (uid)."
        },
        "displayName": {
          "type": "string",
          "description": "The user's chosen display name."
        },
        "email": {
          "type": "string",
          "description": "The user's email address.",
          "format": "email"
        },
        "nativeLanguage": {
          "type": "string",
          "description": "The user's selected native language (e.g., 'English')."
        },
        "selectedLanguage": {
          "type": "string",
          "description": "The target language the user is currently learning (e.g., 'French')."
        },
        "subscriptionType": {
          "type": "string",
          "description": "The user's current subscription level: 'free', 'monthly', or 'yearly'."
        },
        "xpPoints": {
          "type": "number",
          "description": "Total experience points accumulated by the user."
        },
        "currentStreak": {
          "type": "number",
          "description": "The number of consecutive days the user has completed at least one lesson."
        },
        "lastActiveDate": {
          "type": "string",
          "description": "The date of the user's last recorded activity.",
          "format": "date"
        },
        "aiPlanningEnabled": {
          "type": "boolean",
          "description": "A flag indicating if AI plans weekly themes."
        }
      },
      "required": [
        "id",
        "displayName",
        "email",
        "nativeLanguage",
        "selectedLanguage",
        "subscriptionType",
        "xpPoints",
        "currentStreak",
        "lastActiveDate",
        "aiPlanningEnabled"
      ]
    },
    "AdminUser": {
        "title": "AdminUser",
        "type": "object",
        "description": "Represents a user with administrative privileges.",
        "properties": {
            "uid": {
                "type": "string",
                "description": "The Firebase Authentication UID of the admin user."
            },
            "role": {
                "type": "string",
                "enum": ["admin"]
            }
        },
        "required": ["uid", "role"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous",
      "google.com"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/userProfiles/{userId}",
        "definition": {
          "schema": {
            "$ref": "#/entities/UserProfile"
          },
          "description": "Stores individual user preferences and settings."
        }
      },
       {
        "path": "/adminUsers/{userId}",
        "definition": {
          "schema": {
            "$ref": "#/entities/AdminUser"
          },
          "description": "Collection to store admin user roles."
        }
      }
    ]
  }
}
